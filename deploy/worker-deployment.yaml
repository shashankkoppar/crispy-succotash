apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: worker
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: worker
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        io.kompose.service: worker
    spec:
      containers:
        - env:
            - name: APP_TYPE
              value: worker
          image: shashankkoppar/samplecrispynew
          name: worker
      restartPolicy: Always
